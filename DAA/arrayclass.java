public class arrayclass{
    public static void main(String args[]){
        // int [] number = {12, 34, 56, 67, 78};
        // for(int i = 0; i < number.length; i++){

        //    // System.out.print(number[i] +" ");
        //   //  System.out.print (Array.String(number));

        // //There are Three different way to print array
        // // 1. 
        // int [] num; (get memory compile time) // refernce variable(imagine a boy name is  Ali it just a name not a physical existace)
        // num = new int [5]; (get memory in run time) // object variable (it is the body or abilities which is gives to Ali)
        // // here new keyword is DMA (Dynamic memory allocate (memory given in run time))
        // // another type is SMA (Static memory allocation(memory given in compile time))
        // // System.out.print(num); // exception error (value is not specified)
        // // // 2. 
        // int num2 [] = new int [5];// refrence and object variable at same line
        // // let give some value\
        //  num2[0] = 23;
        //  num2[1] = 45;
        //  num2[2] = 88;
        //  num2[3] = 56;
        //  num2[4] = 43;
      
        // System.out.print(num2[0]+" ");
        // System.out.print(num2[1]+" ");
        // System.out.print(num2[2]+" ");
        // System.out.print(num2[3]+" ");
        // System.out.print(num2[4]+" ");

        // refrence variable (get memory in compile time)
        // object variable (get memory in run time)
        // object value store in  heap
        // refrence variable store in stack (first in last out)
        // new keyword use to craete non-primitive datatype it store in heap 
        // heap it is region of memory which take dynamic memory allocation
        // stack is not continous its manage by dynamically

        // for each loop (used for collection no end point no start point java by default run from 0 till end)
        // for(datatype + variable: collection)
        // int arr [] ={23, 34, 56,67};       
        // for(int i:arr);
        
        // modularity (to make seperte logic for every functon)
        // Static mean if our main method is not static than we do dont call static class or method of out side of main method

        // Dynamic memory allocation   => get value at run time   <= non_primative
        // static memory allocatioj    => get value at compile time    <= primative

        // new keyword is used to make non-primative datatype (DMA) 
        




        }
    }
